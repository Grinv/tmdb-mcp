// Kept in sync with package.json by a unit test (see __tests__/version.test.ts).
export const VERSION = "0.7.0";

export const USER_AGENT = `tmdb-mcp/${VERSION} (+https://github.com/Grinv/tmdb-mcp)`;
